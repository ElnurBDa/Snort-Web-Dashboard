# VulnWatch remediation

**CVE / ID:** `CVE-2025-68470`
**Package:** `react-router`
**Suggested fixed version:** `6.30.2, 7.9.6`
**Finding id:** `69e10428e02ea458269b3097`

## Automated suggestion

### Remediation for CVE-2025-68470 in react-router

**Package:** `react-router`
**Installed Version:** `6.21.0`
**Upgrade Target:** `6.30.2`

**Description:**
A vulnerability has been identified in `react-router` version `6.21.0`. Upgrading to version `6.30.2` or higher is recommended to address this issue.

**Steps to Remediate:**
1.  Locate the `package.json` file in your project.
2.  Update the `react-router` dependency to `^6.30.2` (or the latest stable version greater than `6.30.2`).
    Example:
    ```json
    {
      "dependencies": {
        "react-router": "^6.30.2"
      }
    }
    ```
3.  Run your package manager's install command (e.g., `npm install`, `yarn install`, or `pnpm install`) to update the dependency and regenerate the lock file.

**Verification:**
Confirm that `react-router` is updated to `6.30.2` or higher in your `package.json` and lock file.

## Code changes

- Patched: `package.json`, `package-lock.json`

---
*Opened by [VulnWatch](https://github.com/). Review, test, and adjust versions before merge.*
