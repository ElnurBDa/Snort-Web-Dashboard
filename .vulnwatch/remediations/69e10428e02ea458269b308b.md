# VulnWatch remediation

**CVE / ID:** `CVE-2025-27789`
**Package:** `@babel/runtime`
**Suggested fixed version:** `7.26.10, 8.0.0-alpha.17`
**Finding id:** `69e10428e02ea458269b308b`

## Automated suggestion

### Remediation for CVE-2025-27789 in @babel/runtime

**Package:** `@babel/runtime`
**Installed Version:** `7.23.6`
**Upgrade Target:** `7.26.10`

**Description:**
A vulnerability has been identified in `@babel/runtime` version `7.23.6`. Upgrading to version `7.26.10` or higher is recommended to address this issue.

**Steps to Remediate:**
1.  Locate the `package.json` file in your project.
2.  Update the `@babel/runtime` dependency to `^7.26.10` (or the latest stable version greater than `7.26.10`).
    Example:
    ```json
    {
      "dependencies": {
        "@babel/runtime": "^7.26.10"
      }
    }
    ```
3.  Run your package manager's install command (e.g., `npm install`, `yarn install`, or `pnpm install`) to update the dependency and regenerate the lock file.

**Verification:**
Confirm that `@babel/runtime` is updated to `7.26.10` or higher in your `package.json` and lock file.

## Code changes

- Patched: `package.json`, `package-lock.json`

---
*Opened by [VulnWatch](https://github.com/). Review, test, and adjust versions before merge.*
